{
  "name": "quorum-test-network",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run smart_contracts/scripts/deploy.js --network quorum",
    "sync": "NETWORK=quorum ./sync-contracts.sh",
    "release": "npm run compile && npm run deploy && npm run sync"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "3.1.0",
    "@nomicfoundation/hardhat-toolbox": "6.1.0",
    "dotenv": "^17.2.2",
    "hardhat": "2.26.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "web3": "^4.16.0"
  }
}
